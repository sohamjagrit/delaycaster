# Streamlit app runtime. scikit-learn / lightgbm / pandas are PINNED to the AzureML
# training stack (azureml/conda_train.yml) so the committed champion pickle unpickles
# and predicts identically — a version drift silently breaks the OneHotEncoder.
# Target Python 3.11 on Streamlit Community Cloud (Advanced settings → Python version).
streamlit>=1.40
plotly>=5.22
scikit-learn==1.3.2
lightgbm==4.3.0
pandas==2.1.4
pyarrow==14.0.2
joblib>=1.3
